import {} from 'react'

interface Props {
    images: string[]
}

const RightColumnSlider = ({images}: Props) => {
  return (
    <div>
        Slider
    </div>
  )
}

export default RightColumnSlider